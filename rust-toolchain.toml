[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
# Cross-compile targets via: rustup target add <target>
#   aarch64-unknown-linux-gnu (Pi 5) · armv7-unknown-linux-gnueabihf (Pi 3/4)
